const { RequestValidationError } = require('../errDefinitions')

exports.validateProduct = data => {
  /**
   * data must have: 
   * name: not null, not whitespace, alphanumeric
   * price: not null, not whitespace, numeric
   * code: not null, not whitespace, alphanumeric
   * stock: not null, not whitespace, numeric
   */
  try {
    
  }
  catch(err) {
    throw err
  }
}